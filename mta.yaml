_schema-version: "3.2"
ID: materialatpreport
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: materialatpreport-destination-content
  type: com.sap.application.content
  requires:
  - name: materialatpreport-destination-service
    parameters:
      content-target: true
  - name: materialatpreport-repo-host
    parameters:
      service-key:
        name: materialatpreport-repo-host-key
  - name: materialatpreport-uaa
    parameters:
      service-key:
        name: materialatpreport-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: materialatpreport_html_repo_host
          ServiceInstanceName: materialatpreport-html5-service
          ServiceKeyName: materialatpreport-repo-host-key
          sap.cloud.service: materialatpreport
        - Authentication: OAuth2UserTokenExchange
          Name: materialatpreport_uaa
          ServiceInstanceName: materialatpreport-xsuaa-service
          ServiceKeyName: materialatpreport-uaa-key
          sap.cloud.service: materialatpreport
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: materialatpreport-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: materialatpreport-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - materialatpreport.zip
      name: materialatpreport
      target-path: resources/
- name: materialatpreport
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: materialatpreport-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: materialatpreport-destination-service
    service-plan: lite
- name: materialatpreport-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: materialatpreport-xsuaa-service
    service-plan: application
- name: materialatpreport-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: materialatpreport-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
